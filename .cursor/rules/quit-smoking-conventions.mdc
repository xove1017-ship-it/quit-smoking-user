---
description: 本项目编写规范（Vue3 + uni-app + TS 实用主义）
alwaysApply: true
---

# quit-smoking-user 编写规范

## TypeScript（实用、不追求极致严格）

- 项目已关闭 `strict` / `noImplicitAny` / `strictNullChecks`（见根目录 `tsconfig.json`），编写时不必为每个接口字段穷举类型。
- 接口响应、列表项、后端未稳定字段：可用 `any`、`Record<string, unknown>` 或宽松接口，避免为一次性映射写冗长类型。
- 仅在模块边界、公共工具、复用强的 composable 里适度补类型，以可读为准。
- 避免无意义的 `as` 链；若需断言，简短即可。
- `vue-tsc` 能过即可，不必为「理论上更正确」的类型重构业务代码。

## Vue / uni-app

- 使用 `<script setup lang="ts">`，与现有页面一致。
- 路由、存储、请求沿用项目现有写法（`@/api/*`、`@/utils/request`、`uni.*`）。
- 新页面在 `src/pages.json` 注册；路径与文件夹命名保持现有风格（如 `bet_index/bet_index`）。

## 风格与范围

- 改动紧贴需求：不顺带大重构、不批量改无关文件。
- 命名、注释、样式与相邻文件保持一致；业务文案可用中文。
- 用户未要求时不要新增说明类 markdown；**本规则文件**为长期约定例外。

## API 与数据

- **后端返回的数据优先直用**：`data` 里已有、且可直接绑定展示的字段（如 `stats.today_checkin`、`members[].nickname`），优先在模板或少量 `ref` 上直接引用，避免先把整表 `map` 成另一套大对象再渲染。
- 仅在需要统一展示格式、兼容多字段名、拼文案、或兜底默认值时再写小函数 / 局部 `map*`；兼容逻辑可集中在一处，不要为「数据层更纯」多叠一层。
- 后端字段名可能变化：列表/详情映射用可选链与默认值，集中在 `map*` 或 composable 中便于改一处。
- 请求失败以项目现有 `request` 提示为准，不必每层再包 try/catch，除非有特殊分支。

### 接入接口时的必做检查（其二）

每次新增或修改接口调用时，**编写侧须自行核对**，不要只把方法接上就算完成：

- **契约**：路径、HTTP 方法、query/body 参数名与文档或后端约定一致；分页、id、枚举值是否传对。
- **调用逻辑**：是否在正确时机调用（如详情先于时间线、登录态、`onShow` 是否重复请求）；成功/失败分支是否覆盖（空列表、409、业务码非 1）。
- **数据流**：响应里取的字段是否与展示/下一跳路由一致；`redirect`、列表跳转带参是否完整。
- 有条件时在本地或文档中对照跑一遍主路径，发现问题再改代码，而不是依赖用户事后指出。



## 自检

- 改完后在项目根执行 `npm run type-check`；若仅为边缘 any 报错，优先简化类型或局部忽略，而非大改结构。
